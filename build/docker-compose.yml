version: "3"
services:
  workspace_for_build:
    image: workspace_for_build:${IAMGE_VER:-latest}
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: bash
    volumes:
      - .:/workspace
